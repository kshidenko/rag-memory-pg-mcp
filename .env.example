# Supabase Configuration (Required)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key

# Embedding Provider (Optional, defaults to LOCAL)
# Options: LOCAL (HuggingFace, privacy-focused) or OPENAI (fast, cloud-based)
EMBEDDING_PROVIDER=LOCAL

# OpenAI Configuration (Required only if EMBEDDING_PROVIDER=OPENAI)
OPENAI_API_KEY=sk-your-openai-api-key

# Performance Comparison:
# LOCAL:  Slower, Free, Private, ~50MB model download
# OPENAI: 10-100x faster, ~$0.02/1M tokens, Cloud-based
#
# Both produce 384-dimensional vectors (fully compatible)
